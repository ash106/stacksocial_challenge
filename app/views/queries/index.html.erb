<div class="row text-center">
  <div class="col-md-6 col-md-offset-3">
    <h1>All Queries</h1>

    <% @queries.each do |query| %>
      <p><%= link_to query.content, root_path(query: query.content) %> - <%=time_ago_in_words(query.created_at) %> ago</p>
    <% end %>
  </div>
</div>
